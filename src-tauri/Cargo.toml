[package]
name = "passive-tree"
version = "0.1.0"
description = "Passive Tree v0.1 desktop shell (Tauri 2)"
authors = ["Passive Tree"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "passive_tree_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
