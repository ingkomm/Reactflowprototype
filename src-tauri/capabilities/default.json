{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window shell + local video file picker only.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
