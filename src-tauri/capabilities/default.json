{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the main window shell only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
