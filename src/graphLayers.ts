/** Visual stacking contract for Root arena / edges / nodes / interaction. */
export const ROOT_ARENA_Z = 0
export const GRAPH_EDGE_Z = 1
export const GRAPH_NODE_Z = 6
/** CSS z-index for Root socket / Power Core handles within the Root node. */
export const ROOT_INTERACTION_Z = 6
